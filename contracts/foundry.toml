[profile.default]
src = "src"
script = "script"
test = "test"
out = "out"
cache_path = "cache"
libs = ["lib"]

solc_version = "0.8.28"
optimizer = true
optimizer_runs = 200
evm_version = "shanghai"
via_ir = true
bypass_prevrandao = true


[rpc_endpoints]
sepolia = "${SEPOLIA_RPC_URL}"
creditcoin = "${CREDITCOIN_RPC_URL}"

